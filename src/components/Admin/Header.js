import React from "react";
import { Button, Input, Space, Table, Layout, Menu } from "antd";
const { Header, Sider, Content } = Layout;

const AdminHeader = () => {
  return (
    <Header
      className="site-layout-background adminheader"
      style={{
        padding: 0,
      }}
    ></Header>
  );
};

export default AdminHeader;
